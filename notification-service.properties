# --- Specific Configuration for notification-service ---

# Service Name
spring.application.name=notification-service

# Specific Server Port
server.port=8085

# Specific Database URL for notification-service
spring.datasource.url=jdbc:mysql://localhost:3309/notificationdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

# Email Configuration (Specific to this service)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=engineer.ammar18724@gmail.com
# IMPORTANT: Store passwords securely, not directly in config files (e.g., use environment variables or secrets manager)
spring.mail.password=znbnqmlwmenzzxld # Consider externalizing this
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Kafka Configuration (Specific to this service)
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=notification_group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
# Allow deserialization for all packages (adjust for security if needed)
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# Specific Actuator endpoints exposure for this service
# Overrides the default set in application.properties
management.endpoints.web.exposure.include=health,info
